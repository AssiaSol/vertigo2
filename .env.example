REACT_APP_API_URL=http://localhost:5096

# Note: the Groq chatbot key is NOT here anymore — it lives on the backend so it
# never ships to the browser. Set it on the API instead (GROQ_API_KEY env var, or
# Groq:ApiKey in appsettings.Development.json for local dev).
